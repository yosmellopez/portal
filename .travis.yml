language: php

php:
  - 7.3

cache:
  directories:
    - node_modules
    - vendor

before_script:
  - composer install

script: vendor/bin/phpunit --configuration phpunit.xml

notifications:
  email:
    - yosmellopez@gmail.com